# ==================== CONFIGURATION ====================
# Drive letter for the RAM disk (must be available, not in use)
$GAMEDISK = "Z:"

# Size of RAM disk (supports M for MB or G for GB, e.g., "1G", "4G", "512M")
# Recommended: Game size + 20% buffer cause why not
$RAMSIZE = "4G"

# Full path to your 7-Zip (.7z) game archive file
$GAMEARCHIVE = "F:\Games\Gaming Simulator.7z"

# Destination path where 7-Zip extracts files (should be the RAM disk)
$EXTRACTPATH = "Z:\"

# Full path to the game executable inside the archive (must exist after extraction)
$GAMEEXE = "Z:\Gaming Sim.exe"

# Window title for identification (used in display messages)
$GAMETITLE = "Gaming Simulator"

# Process name for tasklist verification (executable name without .exe)
$PROCESSNAME = "gamesim"

# Path to AIM Toolkit command-line executable (aim_ll.exe)
$AIMLL = "C:\Program Files\AIM Toolkit\aim_ll.exe"

# Path to 7-Zip command-line executable (7z.exe)
$7Z = "C:\Program Files\7-Zip\7z.exe"
# ======================================================
